linters: linters_with_defaults(
    line_length_linter(120),
    # object_name_linter = NULL,
    # object_usage_linter = NULL,
    commented_code_linter = NULL)  # Prevent line break before closing paren
exclusions: list(
    "tests/testthat/exclusions-test")  # Prevent line break before closing paren
encoding: "UTF-8"
